{% extends 'include/base.html.twig' %}
{%  block title %}
    {% trans %}Appointment Proposal{% endtrans %}
{% endblock %}
{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="row head-table col-md-12">
        <div class="grid-title-table col-md-9 mt-4">
            <h4 class="h4-content">{% trans %}Appointment proposal{% endtrans %}</h4>
        </div>
        <div class="grid-edit-btn col-md-3 mt-3">
            <button class="btn btn-danger add_form_rdv">{% trans %}+ Create appointment proposal{% endtrans %}</button>
        </div>
    </div>
    <div class="row-table table-content">
        <table class="table table-striped" id="example1">
            <thead>
            <tr>
                <th>{% trans %}Date of proposal appointment{% endtrans %}</th>
                <th>Type</th>
                <th>{% trans %}Description Proposition{% endtrans %} </th>
                <th>{% trans %}Vaccine{% endtrans %}</th>
                <th>Patient </th>
                <th></th>

            </tr>
            </thead>
            <tbody>
            {% for pro in consultation  %}
                {% if pro.descriptionProposition != null  %}
                <tr>
                    <td class="text-left">
                        {{ pro.dateProposition |format_datetime('full', 'short', locale=app.request.locale)}}
                    </td>
                    <td>Consultation</td>
                    <td>
                        {{ pro.descriptionProposition }}
                    </td>
                    <td></td>
                    <td>{{ pro.lastName }} {{ pro.firstName }}</td>

                    <td>
                        <span class="btn btn-primary data-edit" data-id="{{ pro.id }}" ><i class="fa fa-pencil"></i></span>
                        <span class="btn btn-danger data-remove" data-id="{{ pro.id }}" ><i class="fa fa-trash-o"></i></span>
                    </td>

                </tr>
                {% endif %}
            {% endfor %}
            {% for vacc in vaccination  %}
                {% if vacc.vaccinName != null  %}
                <tr>
                    <td class="text-left">
                        {{ vacc.dateProposition |format_datetime('full', 'short', locale=app.request.locale)}}
                    </td>
                    <td>
                       Vaccin </td>
                    <td></td>
                    <td>
                            {{ vacc.vaccinName }}

                    </td>
                    <td>{{ vacc.lastName }} {{ vacc.firstName }}</td>
                    <td>
                        <span class="btn btn-primary data-edit" data-id="{{ vacc.id }}" ><i class="fa fa-pencil"></i></span>
                        <span class="btn btn-danger data-remove" data-id="{{ vacc.id }}" ><i class="fa fa-trash-o"></i></span>
                    </td>

                </tr>
                {% endif %}
            {% endfor %}
            </tbody>
        </table>
    </div>


{% endblock %}

{% block javascripts %}
    <!-- DataTables -->
    <script>
        $(".add_form_rdv").on('click' , function(e) {
            $(".loadBody").css("display", "block");
            e.preventDefault();
            $.ajax({
                url: "{{ path('add_form_proposition_rdv') }}",
                type: 'post',
                data: {
                    action: 'new'
                },
                dataType: 'json',
                success: (data) => {
                    $('.create_body').html(data.form_html);
                    $("#modal_create_body").modal('show');
                    $('#proposition_rdv_heureRdv').clockpicker({
                        donetext: "valider",
                        autoclose: true,
                    });
                    $('#proposition_rdv_heureRdv').on('change', function () {
                        let receivedVal = $(this).val();
                        $(this).val(receivedVal);
                    });

                    $(".loadBody").css("display", "none");
                },
                error: (e) => {
                    $(".loadBody").css("display", "none");
                    $(".chargementError").css("display", "block");
                }
            }).done(() => {
                $(".loadBody").css("display", "none");
            });
        });
        $('.data-edit').on('click', function (e) {
            e.preventDefault();
            var id = $(this).data('id');
            $.ajax({
                url: "{{ path('add_form_proposition_rdv') }}",
                type: 'post',
                data: {
                    id: id,
                    action: 'edit'
                },
                dataType: "json",
                success: (data) =>{
                    $('.create_body').html(data.form_html);
                    $("#modal_create_body").modal('show');
                    $('#proposition_rdv_heureRdv').clockpicker({
                        donetext: "valider",
                        autoclose: true,
                    });
                    $('#proposition_rdv_heureRdv').on('change', function () {
                        let receivedVal = $(this).val();
                        $(this).val(receivedVal);
                    });
                    $(".loadBody").css("display", "none");
                },
                error: (e) => {
                    $(".loadBody").css("display", "none");
                    $(".chargementError").css("display", "block");
                }
            }).done(() => {
                $(".loadBody").css("display", "none");
            });
        });

        $('.data-remove').on('click', function (e) {
            e.preventDefault();
            var $delete = confirm('Êtes-vous sûr de vouloir supprimer cet rdv ?');
            if($delete){
                $(".loadBody").css("display", "block");
                var id = $(this).data('id');
                $.ajax({
                    url: "{{ path('remove_proposition') }}",
                    type: 'post',
                    data: {
                        id: id,
                    },
                    dataType: "json",
                    success: (data) =>{
                        $(".loadBody").css("display", "none");
                        location.reload();
                    },
                    error: (e) => {
                        $(".loadBody").css("display", "none");
                        $(".chargementError").css("display", "block");
                    }
                }).done(() => {
                    $(".loadBody").css("display", "none");
                });
            }else {
                return false
            }
        });
        $(document).ready(function() {
            $('#example1').DataTable()
            $('.date_rdv').datepicker({
                format: 'dd/mm/yyyy',
                autoclose: true,
            })
        });
    </script>
{% endblock %}